gameplay polish: hold-to-crouch + footsteps + tutorial cleanup

- PlayerInputHandler: CTRL is now hold-to-crouch (HL style); auto-stand
  when ceiling clears via existing CanStandUp() check.
- FootstepPlayer3D + SoundManager: step interval and volume now adapt
  to walk / sprint / crouch (sprint cadence ~0.30s, walk ~0.45s,
  crouch ~0.55s @ 45% volume).
- MainScene_3D: tutorial sign updated from "press to Crouch" to
  "HOLD CTRL TO CROUCH" to match new behavior.
- Bug_01/02/03: English label cleanup (Red Door, Key:, Ladder N - Rotation:).
